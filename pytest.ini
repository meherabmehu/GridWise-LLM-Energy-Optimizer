[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    live: requires a configured language-model provider (skipped without an API key)
filterwarnings =
    ignore::DeprecationWarning
